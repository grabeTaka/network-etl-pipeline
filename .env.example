# MongoDB connection string (host should match the docker service name)
DB_URI=mongodb://database:27017/ozmap_isp

# Name of the MongoDB database
DB_NAME=ozmap_isp

# Application port (must match the exposed port in docker-compose)
APPLICATION_PORT=3000

# Hostname used to access the application (localhost for local dev)
APPLICATION_HOST=localhost
